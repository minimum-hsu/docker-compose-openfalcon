mysql:
  container_name: mysql
  image: minimum/openfalcon-mysql
  expose:
    - "3306"
  environment:
    - MYSQL_ROOT_PASSWORD=password

redis:
  container_name: redis
  image: redis:3.0
  expose:
    - "6379"
